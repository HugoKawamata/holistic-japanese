/**
 * @flow
 */

const url = `https://www.issei.com.au/graphql/`;

module.exports = {
  client: {
    service: {
      url,
      name: "Issei",
      // headers: {
      //   authorization: `Bearer ${API_KEY}`,
      // },
    },
  },
};
